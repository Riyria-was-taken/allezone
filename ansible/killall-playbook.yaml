---
- name: Stop and clean all containers 
  hosts: all

  tasks:
     - name: docker stop 
       command:
          argv:
            - docker
            - stop
            - $(docker ps -a -q) 

     - name: docker rm
       command:
          argv:
            - docker
            - rm
            - $(docker ps -a -q) 
